# Given what we know about about factoring this system to work with ollama: 
- Generate a compelling visualy intersting document describing how to factor thi ssystem to use ollama as a provider in addition to the current openai and anthropic. 
- Ensure ability to configure the context window and the outpt limit to the max allowed by the given ollama model. 
- The default system should use qwen3-coder model. The design should assume that the ollam server is already running and the ollam server is already running the qwen3-coder model.
- The document will assume that the user is running a 4090 or a 5090 rtx card.